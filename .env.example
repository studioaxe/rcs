# ============================================================================
# Rental Calendar Sync v4.0 - .env.example
# ============================================================================
# INSTRUÇÕES:
# 1. Copie este ficheiro para .env
# 2. Preencha os valores reais (NUNCA fazer commit do .env)
# 3. Valores sensíveis: FLASK_SECRET_KEY, GITHUB_TOKEN, EMAIL_PASSWORD, etc.
# 4. Para Render: definir variáveis no painel (Environment → Secrets)
#
# VERSÃO: 4.0
# DATA: 10 Janeiro 2026
# ============================================================================

# ============================================================================
# FLASK / WEB
# ============================================================================
FLASK_ENV=production
FLASK_SECRET_KEY=seu-secret-key-super-seguro-aqui
FLASK_DEBUG=0
WEB_USERNAME=admin
WEB_PASSWORD=change-me
SESSION_TIMEOUT_MINUTES=120

# ============================================================================
# RENDER / STORAGE
# ============================================================================
# IMPORTANTE: Para persistência em Render, configurar Render Disk em /mnt/data
RENDER=true
REPO_PATH=/mnt/data
DATA_DIR=/mnt/data
ICS_SOURCE=local
# Opções: "local" (Render cria/leitura local) ou "github" (lê do repositório)
ICS_CACHE_DIR=.ics_cache

# ============================================================================
# CALENDÁRIOS (iCal URLs)
# ============================================================================
AIRBNB_ICAL_URL=https://www.airbnb.com.br/calendar/ical/SEU_TOKEN.ics
BOOKING_ICAL_URL=https://ical.booking.com/v1/export/t/SEU_TOKEN.ics
VRBO_ICAL_URL=https://www.vrbo.com/icalendar/SEU_TOKEN.ics

# ============================================================================
# TEMPO DE PREPARAÇÃO (TP Antes/Depois)
# ============================================================================
BUFFER_DAYS_BEFORE=1
BUFFER_DAYS_AFTER=1

# ============================================================================
# GIT HUB API (para disparar workflow)
# ============================================================================
# Token pessoal: https://github.com/settings/tokens → "repo", "workflow"
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_OWNER=studioaxe
GITHUB_REPO=rental-calendar-sync-3.0
GITHUB_WORKFLOW=full_auto_workflow.yml
GITHUB_BRANCH=main

# ============================================================================
# EMAIL (Gmail, Outlook, etc.)
# ============================================================================
EMAIL_SERVER=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu-email@gmail.com
EMAIL_PASSWORD=app-password-gmail  # https://myaccount.google.com/apppasswords
NOTIFICATION_EMAIL=seu-email@gmail.com
ERROR_EMAIL=seu-email@gmail.com
EMAIL_ON_ERROR=true
EMAIL_ATTACH_LOG=true

# ============================================================================
# CORS / WEB
# ============================================================================
CORS_ORIGINS=rentalcalendarsync.onrender.com,localhost:5000,127.0.0.1:5000
PORT=8000  # Render usa $PORT, mas definir por segurança

# ============================================================================
# LOGGING
# ============================================================================
LOG_LEVEL=INFO
LOG_FILE=sync.log

# ============================================================================
# DESENVOLVIMENTO LOCAL (opcional)
# ============================================================================
# Para testes locais, comentar RENDER=true e usar:
# RENDER=false
# REPO_PATH=./data
